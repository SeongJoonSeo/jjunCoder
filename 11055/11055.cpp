#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main()
{
    int N;
    cin >> N;
    vector<int> sum;
    sum.assign(N, 0);
    for (int i = 0; i < N; ++i)
    {
        int num;
        cin >> num;
    }

    return 0;
}
