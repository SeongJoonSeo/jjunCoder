#include <iostream>
#include <algorithm>
#include <string>

using namespace std;

int main(){
	string n;
	cin.sync_with_stdio(false);
	cin >> n;
	reverse(n.begin(),n.end());
	int len = 0;
	for(int i=0;i<n.size();i++){
		int temp = n[i];

	}
	return 0;
}